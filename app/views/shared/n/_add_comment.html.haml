- edit ||= false
- collapsed = false #@comment_body.nil?  && f.object.errors.empty?
= subtitle :comment, collapsed, t(:comment)
.row
  .col-md-12
    .form-group
      %small#comment_intro{ hidden_if(!collapsed) }
        = t(:comment_intro)
      #campaign_comment{ hidden_if(collapsed) }
        = text_area_tag :comment_body, @comment_body, :class => 'form-control'
